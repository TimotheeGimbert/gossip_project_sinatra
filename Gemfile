source 'https://rubygems.org'
ruby '2.7.4'
gem 'rspec' # testing program
gem 'pry' # pry allows pausing execution
gem 'sinatra' # sinatra is a low-level ruby framework for web Apps
gem 'thin' # thin is recommended by sinatra
gem 'shotgun' # shotgun allows a dynamic local server
gem 'csv'
